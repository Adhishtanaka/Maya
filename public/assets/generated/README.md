# Retained original project geometry

The current city loads `pine.glb`, `pine-frost.glb` and `lantern.glb`. These retained models were authored as original box geometry for an earlier iteration of this project; they do not contain downloaded samples. The earlier generator and unused models are no longer distributed. See `docs/asset-audit.json` and `THIRD_PARTY.md` for the current asset inventory.
